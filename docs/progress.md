# 开发进度

最后更新：2026-07-21

## 阶段状态

| 阶段 | 状态 | 说明 |
|---|---|---|
| 1. 工程骨架 | 已完成 | Vue/Tauri 双窗口、托盘、定向通信、测试与图标骨架已建立 |
| 2. 悬浮球与花瓣 | 已实现，待平台复验 | 状态机、拖动、四边吸附、半隐藏、扇形布局、动画与点击穿透原型已完成 |
| 3. 通用面板框架 | 已实现，待实机复验 | 五类容器、内部路由、标题栏、主题与通用反馈组件已完成 |
| 4. 开发转换 | 已实现，待实机复验 | JSON、URL、Base64、时间戳、JWT、UUID 六项工具已完成 |
| 5. 健康提醒 | 已实现，待实机复验 | 五类配置、调度、勿扰、静默、提醒卡和完成统计已完成 |
| 6. 剪贴板 | 已完成 | 按最新范围完成文本、图片、搜索、收藏、清理与敏感保护；文件列表、来源应用和排除应用不再开发 |
| 7. 文件与路径 | 已完成 | 常用位置、路径工具、日期文件夹和临时中转均已实现；待 Windows 11 复验 |
| 8. 快捷入口与设置 | 已完成，待平台复验 | 快捷入口、系统设置、全局快捷键、双击动作、开机启动、模式联动和数据清理已完成 |
| 9. 质量与打包 | macOS 范围已完成 | 全量检查与 macOS `.app` 打包通过；Windows 安装包和 Windows 11 实机验收待执行 |

## 已确认技术决策

- 前期提供可在 macOS 运行的版本，同时保留 Windows 11 为第一版验收目标。
- 剪贴板第一版范围于 2026-07-21 收口为文本与图片，不再开发文件列表、来源应用识别和排除应用。
- SQLite 与缓存文件不做静态加密；敏感剪贴板内容默认不落盘。
- 提醒边界行为采用说明书评审时给出的默认方案。
- 花瓣透明窗口优先验证原生命中测试，必要时改用临时多窗口方案。
- 用户自选应用由受控 Rust command 校验并启动，不开放任意 shell 字符串。

## 验证标记

- `macOS 已验证`：在当前开发设备实际运行通过。
- `跨平台代码已实现`：完成实现与自动化测试，但尚未在目标系统实测。
- `等待 Windows 验证`：涉及 Windows 窗口、剪贴板、快捷键、启动或打包能力。

## 阶段 1 验证记录

2026-07-21：

- `pnpm typecheck`：通过。
- `pnpm test`：通过，2 个前端单元测试。
- `cargo test --manifest-path src-tauri/Cargo.toml --offline`：通过，1 个 Rust 单元测试。
- `pnpm build`：通过。
- `pnpm test:e2e`：通过，2 个浏览器窗口预览场景。
- `cargo check --manifest-path src-tauri/Cargo.toml --offline`：通过。
- `pnpm tauri dev`：macOS 原生编译完成并启动应用进程。
- Windows 双窗口、托盘外观和安装包：等待 Windows 11 设备验证。

## 阶段 2 实现与验证记录

2026-07-21：

- 悬浮球拖动阈值为 5 px，拖动完成后吸附当前显示器最近边缘。
- 四边吸附使用约 224 ms ease-out 原生窗口位置动画。
- 空闲 3 秒后向吸附边缘隐藏 35%，悬停恢复。
- 展开方向按当前显示器左右半区和上下安全空间选择；角落采用交错半径避免标签重叠。
- 花瓣展开/收回使用 42 ms 错峰、缩放、位移、透明度与轻微模糊过渡。
- 动画期间重复点击由状态机拒绝；支持 `prefers-reduced-motion`。
- 透明窗口点击穿透原型已实现，仅主球与花瓣热区接收事件。
- 面板优先出现在悬浮球内侧，并限制在当前显示器 80% 工作区内。
- 吸附位置写入 Tauri Store；多显示器恢复时选择包含该位置的工作区。
- 前端单元测试增至 8 个；浏览器 E2E 增至 4 个；Rust 单元测试增至 2 个。
- macOS Tauri 原生编译和应用启动通过；拖动手感、点击穿透与托盘交互仍建议由用户在桌面实测。
- Windows 原生命中行为、多显示器缩放与吸边动画等待 Windows 11 设备复验。

## 阶段 2 体验修正

2026-07-21：根据 macOS 实机截图完成以下修正：

- 主悬浮球阴影由大范围扩散改为紧凑柔和阴影，避免在 88px 透明窗口下沿被裁切。
- 移除 pointer-down 阶段的半隐藏位置恢复，避免窗口在按下与松开之间移动导致首次 click 丢失；展开流程内部仍会先安全恢复再扩窗。
- 分类花瓣主体由 58px 缩小为 50px，标签和整体点击容器同步缩小。
- 四角展开不再依赖拥挤的等半径 90° 扇形，改用对称五点弧线坐标，任意相邻中心距离大于 70px。
- 右下角展开视觉回归与 4 个 E2E 场景通过。
- 后续实测反馈中，将角落花瓣改为以主球为圆心的对称四分之一圆，内外半径交替但相邻间距一致。
- 主球不再依赖浏览器合成的 `click`：pointer-down 同步捕获，pointer-up 在未超过 5px 时直接执行展开，避免首次点击丢失。
- 拖动捕获改为同步建立；超过 5px 后进入 dragging，松开吸边且不会误触展开，并增加 grab/grabbing 光标。
- 浏览器 E2E 增至 5 个，覆盖“拖动不展开、拖动后仍可首次单击展开”。
- 顶部与底部吸附改为明确的垂直边布局：主球在展开窗口中水平居中，分别向下、向上展开对称半圆；底部布局的标签置于图标上方以避开内侧相邻花瓣，左右边与四角布局保持不变。
- 本次修正后前端 10 项单元测试、Rust 4 项单元测试、浏览器 6 个 E2E 场景全部通过。

## 阶段 3 实现与验证记录

2026-07-21：

- 五个分类共用独立 `panel-window` 容器，并按分类切换标题、图标、点缀色、说明与占位功能卡片。
- 面板导航同步 Vue Router 内部路由；设置页使用同一窗口切换，不创建传统多页面主窗口。
- 通用标题栏支持返回花瓣、分类收藏、打开设置和关闭面板，标题区域可拖动原生窗口。
- 主悬浮球在面板打开期间切换为关闭图标和对应无障碍名称。
- 返回按钮关闭面板并重新展开花瓣；关闭按钮和 Esc 关闭面板但不展开花瓣。
- 已加入系统/浅色/深色主题框架，以及 Toast、Confirm、Tooltip 通用组件；减少动画偏好继续由全局样式统一降级。
- 面板内容在小屏幕下自动切换单列并内部滚动，原生窗口尺寸仍由分类配置且限制在当前工作区 80% 内。
- `pnpm typecheck`、`pnpm build` 通过；前端 12 项单元测试、Rust 4 项单元测试通过。
- 浏览器 E2E 增至 8 个场景，覆盖五类面板路由、收藏提示、主题切换、返回以及 Esc 关闭。
- 560×540 面板视觉截图已检查；原生窗口拖动、焦点与五类窗口尺寸建议在 macOS 实机继续复验。
- macOS 实机反馈中将面板的大范围投影改为带负扩散的紧凑双层阴影，并把透明窗口内边距增至 12px，避免四周及底部出现明显裁切。
- 花瓣完全展开后，悬浮球窗口失去焦点会走正常收回状态机，避免花瓣长期停留在桌面，同时规避展开动画与窗口切换之间的竞态。
- 空闲吸边半隐藏与悬停恢复由瞬时位置切换改为约 224ms 的原生 ease-out 窗口动画，并在前端串行化边缘动画以避免重复触发抖动。

## 阶段 4 实现与验证记录

2026-07-21：

- 开发转换面板使用横向图标切换器，提供 JSON、URL、Base64、时间戳、JWT、UUID 六项工具，不产生二级花瓣。
- JSON 支持 2/4 空格格式化、压缩、校验、错误行列提示、输入输出交换、剪贴板填充和清空；超过 2 MB 后自动交给 Web Worker。
- URL 支持组件编码、完整 URI 编码、解码、查询参数与 JSON 双向转换，并提示输入是否可能已编码。
- Base64 支持 UTF-8 文本编解码、Data URL 转换、图片选择和预览；图片超过 5 MB 时显示风险提示但允许继续。
- 时间戳支持自动识别 10/13 位、日期时间双向转换、本地时区、UTC 和当前时间。
- JWT 仅在本地解析 Header/Payload，展示 exp、iat、nbf 和过期状态，明确提示不验证签名且不保存输入。
- UUID 支持一次生成 1/5/10/20 个 v4 值、去除连字符、转大写以及单项/全部复制。
- Ctrl/Cmd+Enter 执行、Ctrl/Cmd+Shift+C 复制结果、Ctrl/Cmd+K 聚焦当前输入区。
- 面板根容器固定在原生窗口范围内，输入聚焦不会把自定义标题栏滚出可视区域。
- `pnpm typecheck`、`pnpm build` 通过；前端单元测试增至 24 项，其中开发转换专项 12 项。
- 浏览器 E2E 增至 10 个场景，覆盖六工具入口、JSON 格式化和真实剪贴板复制。
- 根据实机反馈将开发转换面板的正文、字段标签、输入输出和按钮文字统一上调约 1px，在不改变窗口尺寸的前提下改善可读性。

## 阶段 5 实现与验证记录

2026-07-21：

- 已实现起立活动、喝水、提肛训练、远眺护眼、坐姿调整五类提醒及说明书默认启用状态、间隔和稍后时间。
- 每项提醒可独立启停并调整间隔/稍后时间，修改后立即重新计算 `nextTriggerAt`；配置通过 Tauri Store 持久化。
- 完成、稍后和自动隐藏记录通过 repository 层写入本地 SQLite，健康面板展示今日分项与总完成次数。
- 支持暂停 30 分钟、暂停 1 小时、今日不再提醒、全局静默和可跨午夜的勿扰时段。
- 多项同时到达时按“起立、护眼、喝水、坐姿、提肛”优先级在一张卡片中依次处理，不叠加窗口。
- 设备长时间挂起后恢复只展示最高优先级一项，其余错过提醒重新排期，不集中补发。
- 到点时原生悬浮窗口扩展显示轻量提醒卡，主球进入青绿色呼吸状态；完成和稍后均可直接操作。
- 卡片 30 秒无操作自动缩回并记录 `auto_hidden`，主球保留未处理状态点，点击可再次查看。
- 提醒卡沿左右/上下吸附方向自动放置，保持在 340×340 透明窗口安全范围内，热区支持原生点击穿透。
- 健康面板默认 520×560，支持内部滚动；默认宽度下保留每项“间隔/稍后”配置字段。
- 新增官方 Tauri SQL 插件和 `reminder_logs` 迁移，capability 仅开放 SQL 默认、查询和执行权限。
- 根据实机反馈将健康面板与悬浮提醒卡的正文、配置标签、统计和按钮文字整体上调约 1px，提高默认尺寸下的可读性。
- 修正深色主题下勿扰时间区域被双列布局压缩的问题：静默与勿扰改为上下通栏卡片，勿扰说明、开关和两个时间输入使用稳定横向网格。
- 五个提醒配置项新增“调试”按钮：点击后关闭面板并向悬浮球发送即时预览事件，绕过静默/勿扰与启用状态，但不修改正式提醒排期。
- 原生实测发现 panel WebView 的前端定向事件未可靠触发，调试链路改为单个受控 Rust command，由主进程原子完成隐藏面板并把提醒 ID 随关闭事件送达悬浮球。

## 阶段 6 第一轮实现与验证记录

2026-07-21：

- 接入 Tauri 官方 clipboard-manager 插件；仅 panel-window 获得文本读取和写入权限，UI 不直接调用系统插件。
- 隐藏的面板 WebView 启动后以 700ms 间隔监听文本剪贴板；暂停记录时跳过读取处理，应用自身回写带短时忽略标记，避免重复入库。
- 新增 `clipboard_items` SQLite 迁移及 repository 层；文本原文、预览、类型、哈希、时间、复制次数、收藏和固定状态均本地保存。
- 去重严格比较最近一条记录的哈希与原始文本；不 trim、不修改换行，完全相同则仅更新时间和复制次数。
- 增加 URL、JSON、代码、颜色和邮箱展示标签识别；标签不改变原始内容。
- 增加私钥、常见访问令牌、密码赋值、验证码和通过 Luhn 校验的银行卡号检测；默认跳过且不持久化正文，关闭保护前显示风险确认。
- 剪贴板面板已支持日期分组、搜索、类型/收藏筛选、暂停、点击复制、收藏、固定、单项/批量删除及清空非收藏记录。
- 普通文本默认 30 天过期，收藏和固定项不参与过期清理；启动时执行一次低优先级清理。
- 前端单元测试增至 36 项，新增 3 项剪贴板分类、敏感检测与精确去重测试；专项浏览器 E2E 覆盖文本卡片、复制、收藏和搜索。
- 第一轮结束时图片和数量上限清理尚未实现，已在第二轮补齐；原计划的文件列表、来源应用识别和排除应用随后由用户明确移出第一版范围。
- 根据实机可读性反馈新增全局排版令牌：正文 14px、紧凑正文 13px、辅助说明与按钮 12px；剪贴板内容卡片和策略区已应用，后续新模块以此为最低字号基线。
- 修正关闭敏感内容保护时确认弹框等待持久化而无法及时消失的问题：确认后立即关闭弹框，保存成功显示明确状态提示，失败则恢复保护开关并提示原因。
- 应用内点击历史卡片重新复制时只更新 `lastCopiedAt` 与复制次数，不再修改用于排序的 `updatedAt`，避免卡片操作后跳到第一位；外部连续复制的去重记录仍正常更新时间。
- 文本卡片预览区改为固定高度的内部滚动区域，长内容可通过滚轮或触控板继续查看，同时保持卡片网格尺寸稳定。
- 修复剪贴板敏感保护与健康设置无法在原生窗口持久化的问题：为 panel-window 和 orb-window 补充 Tauri Store 的 load/get/set/save 最小权限，不开放删除、清空或枚举等无关能力。
- 阶段 6 第二轮加入图片剪贴板：macOS 使用 NSPasteboard `changeCount`、Windows 使用 `GetClipboardSequenceNumber` 判断系统内容是否变化，只有变化后且无文本内容时才读取完整图片。
- 图片以 PNG 保存到应用缓存目录，SQLite 仅记录相对路径、像素尺寸和元数据；面板新增图片筛选与等比缩略图卡片，并支持复制、收藏、固定、单项/批量删除。
- 图片重新复制使用缓存文件还原 RGBA 后写回系统剪贴板，并使用内部图片哈希忽略标记避免自身复制重复入库。
- 过期、手动删除及数量上限清理会同步移除图片缓存；默认图片保留 7 天、最多 200 条，普通记录最多 2000 条，收藏和固定项不参与自动淘汰。
- Tauri capability 仅为 panel-window 开放图片剪贴板读写及应用缓存目录递归读写，不授予用户目录访问权限。
- 前端单元测试增至 37 项，新增图片像素与尺寸哈希测试；浏览器专项 E2E 已覆盖文本与图片卡片混排显示。macOS 原生应用编译并重新启动，真实图片复制手感仍待用户实机复验。
- 明确图片历史以第三方和系统截图工具为主要入口：截图确认后若位图写入系统剪贴板即自动收录，不额外重复开发截图工具；面板空状态已增加对应操作提示。
- 微信截图实测确认其写入标准 `public.tiff`，未收录并非格式不兼容：监听器原位于隐藏 panel WebView 且图片资源缺少 rgba/size/close 权限。唯一轮询器已迁移到常驻 orb-window，并补齐最小图片读取与资源释放 capability；设置和历史通过窗口事件同步。
- 图片卡片交互拆分为“缩略图点击预览、底部复制图标写入剪贴板”：预览层支持关闭按钮、Esc 与点击遮罩关闭，避免用户查看图片时意外覆盖当前剪贴板。
- 用户确认剪贴板以现有文本与图片能力正式收口，文件列表、来源应用识别和排除应用不再开发，阶段 6 标记完成。

## 阶段 7 第一轮实现与验证记录

2026-07-21：

- 文件与路径面板采用常用位置、路径转换、日期文件夹和临时中转四项顶层切换，沿用正文 14px、紧凑正文 13px、说明与按钮 12px 的排版基线。
- 常用位置自动解析当前系统的下载、桌面和文档目录；用户可通过原生目录选择器添加自定义目录，并支持重命名、删除和上下排序。
- 每次打开目录前由受控 Rust command 重新检查路径是否存在且确为目录；失效路径显示禁用状态和明确提示。
- 路径工具支持 Windows 反斜杠转 Unix 斜杠、Windows 盘符转 WSL `/mnt/<drive>/`，以及仅对可识别 WSL 路径执行反向转换。
- 路径核心补充跨分隔符文件名和父目录提取函数，为下一轮拖入文件后的复制操作复用。
- 日期文件夹支持 `YYYY-MM-DD`、`YYYYMMDD` 和 `YYYY-MM-DD_主题`；主题会拒绝路径分隔符及系统保留字符。
- 创建目录由 Rust 再次校验父目录与单级文件夹名称，拒绝路径穿越；同名目录不覆盖，并直接打开现有目录。
- 新增官方 Tauri dialog 插件，仅开放目录选择权限；目录打开和创建不经过任意 shell 字符串。
- 前端单元测试增至 41 项，新增 4 项路径转换、路径拆分和日期命名测试；Rust 测试增至 5 项；专项 E2E 覆盖常用目录、WSL 转换和日期主题预览。
- 620×620 浅色面板视觉检查通过，macOS 原生编译和启动通过；真实目录选择、打开及创建行为待用户实机复验。
- 第一轮暂未实现临时文件中转、拖入路径复制与自动清理；这些能力已在下述第二轮补齐。

## 阶段 7 第二轮实现与验证记录

2026-07-21：

- 路径工具接入原生文件拖放：拖入文件或文件夹后立即复制完整路径，并提供文件名、父目录和完整路径三个明确复制入口。
- 临时中转支持拖入文件与文件夹，也可通过原生选择器添加文件或目录；内容复制到应用数据目录的 `temporary-transfer` 隔离区，不移动源项目。
- 中转元数据通过 SQLite 持久化，展示名称、原始路径、文件或目录类型、递归大小、添加时间和默认 7 天过期时间。
- 固定项不参与过期清理；常驻悬浮球启动时以低优先级清理已过期的未固定项，进入文件模块时也会兜底检查。单项删除先删除中转副本再删除元数据，原始文件不在删除命令作用域内。
- Rust 复制命令在后台线程运行；目标目录只能使用 UUID 标识。递归复制拒绝目录内的符号链接，删除与打开操作均限制在应用临时区，避免路径穿越。
- 新增临时中转 SQLite migration、文件大小与过期时间纯函数测试；Rust 测试增至 6 项，覆盖中转标识路径校验。
- 浏览器专项 E2E 增加中转条目、大小信息和固定交互覆盖。真实大文件/目录拖放、7 天到期清理及 Windows 11 行为仍需实机复验。

## 阶段 8 第一轮实现与验证记录

2026-07-21：

- 快捷入口面板采用“最近与收藏、快捷复制、网址与应用”三项顶层切换，保持 560×540 默认窗口内完整显示。
- 一键复制支持当前日期、当前时间、日期时间、秒级时间戳、毫秒级时间戳和 UUID；操作后显示明确反馈并写入最近使用。
- 最近使用记录通过 SQLite 自动维护，最多显示 8 项；相同功能再次使用时更新次数并提升到第一位。
- 分类收藏和主题由原先的内存状态改为 Tauri Store 持久化，收藏分类会在快捷入口中显示。
- 常用网址支持名称和点缀色，只接受完整 HTTP/HTTPS URL；前端和 Rust command 进行双重协议校验。
- 常用应用通过原生选择器添加，启动前由 Rust 校验路径和平台可启动类型；macOS 允许 `.app` 包或具有执行权限的文件，Windows 适配层仅允许 `.exe` 与 `.lnk`，不执行用户拼接的 shell 字符串。
- 常用网址与应用支持删除和上下排序；应用路径失效后禁用入口并显示明确状态。
- 常用网址卡片新增编辑入口：点击铅笔图标后回填名称、URL 与点缀色，可保存或取消；修改后的 URL 继续执行协议校验和重复检查。
- “网址与应用”页面新增常用网址/常用应用二级切换，两类入口分别展示添加方式、列表、空状态和分类内排序，不再混排。
- 新增 `recent_features` SQLite migration、快捷值和 URL 校验纯函数测试；前端单测增至 44 项，Rust 测试增至 7 项。
- 类型检查、生产构建及快捷入口专项 E2E 通过，560×540 浅色界面视觉检查通过；真实浏览器/应用启动以及 Windows 11 外部程序行为待实机复验。
- 全局快捷键、悬浮球双击动作、开机启动、工作模式和本地数据清理留到阶段 8 下一轮设置开发。

## 阶段 8 第二轮实现与验证记录

2026-07-21：

- 设置面板拆分为常规、行为、数据与应用三页，支持系统/浅色/深色主题、48～68px 悬浮球尺寸和 60%～100% 透明度。
- 接入 Tauri autostart 与 global-shortcut 官方插件；开机启动默认关闭，全局快捷键默认 `Ctrl+Alt+Space`。
- 快捷键输入执行规范化与冲突校验；更换时先注册新组合，再释放旧组合，注册失败保留原组合。
- 悬浮球双击可配置为快捷入口、剪贴板或开发转换；未配置时保持单击立即展开，配置后使用 280ms 区分单击与双击。
- 工作、静默、完全暂停三种模式统一联动托盘、健康提醒和剪贴板监听；模式变化通过 Store 持久化，并在悬浮球显示状态标识。
- 本地数据支持“清除并保留设置”和“清除并重置设置”两种二次确认操作；清理剪贴板与图片缓存、提醒统计、临时中转副本和最近使用，不删除用户原始文件。
- 重置会同时恢复主题、模块设置、快捷键和工作模式，并关闭开机启动；设置页提供明确的应用退出入口。
- 新增设置与快捷键单元测试和模式/双击/数据清理 E2E；本轮结束时前端单元测试共 46 项、Rust 测试 7 项、浏览器 E2E 18 项，全部通过。
- macOS 原生启动时默认全局快捷键注册和插件初始化无报错；开机启动实际写入及快捷键与其他应用冲突仍建议由用户按需实测，Windows 行为等待 Windows 11 复验。

## 阶段 9 质量与打包记录

2026-07-21：

- `pnpm typecheck`、`pnpm test`、`pnpm build`、`pnpm test:e2e` 全部通过；前端为 46 个单元测试和 18 个浏览器交互场景。
- `cargo test` 通过 7 项 Rust 测试，`cargo clippy -- -D warnings` 无警告。
- 最终 Vite 生产主脚本为 299.74 kB（gzip 98.14 kB），样式为 63.37 kB（gzip 9.85 kB），未发现异常体积增长。
- `pnpm tauri build --bundles app` 在当前 Apple Silicon macOS 设备通过，生成包含应用图标的 `花瓣工具箱.app`，应用包约 17 MB。
- 已补齐 README、架构说明、用户说明和发布检查清单，并明确本地数据范围、退出方式和平台限制。
- 尚未执行 Windows 11 x64 的原生窗口、多显示器、WebView2、全局快捷键、开机启动、外部应用启动、NSIS/MSI 安装与卸载验证；不得据此发布 `1.0.0`。
